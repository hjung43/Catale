spring:
  profiles:
    active: dev
    include:
      - jwt
      - filter
#      - db
      - dev

  security:
    user:
      name: user
      password: 1234

  jpa:
    open-in-view: false

logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure:
            security: ERROR
        data:
          repository:
            config:
              RepositoryConfigurationExtensionSupport: ERROR

